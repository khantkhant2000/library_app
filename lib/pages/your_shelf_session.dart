import 'package:flutter/material.dart';

class YourShelf extends StatelessWidget {
  const YourShelf({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("No Data"),
      ),
    );
  }
}
